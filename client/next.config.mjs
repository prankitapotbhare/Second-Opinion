/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'ui-avatars.com',
          // Add any other domains you're using for images
        ],
      },
};

export default nextConfig;
